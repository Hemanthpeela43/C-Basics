#include<stdio.h>
int main()
{
	int n;
	printf("enter a number to check  whether it is positive or negitive");
	scanf("%d",&n);
	if(n>0)
	{
		printf("%d is positive numbeer",n);
	}
	else
	{
		printf("%d is negitive numbeer",n);
	}
}
